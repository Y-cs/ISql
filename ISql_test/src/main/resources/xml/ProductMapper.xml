<mapper namespace="product">

    <select id="selectList" resultType="self.pojo.Product">
        select * from product
    </select>

    <select id="selectOne" resultType="self.pojo.Product"  parameterType="self.pojo.Product">
        select * from product where id=#{id}
    </select>


</mapper>